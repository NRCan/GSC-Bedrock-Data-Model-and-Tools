<controls:ProWindow x:Class="BedrockEditorPro.ProWindows.Form_Help"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BedrockEditorPro.Properties"
        xmlns:bedrockcontrols="clr-namespace:BedrockEditorPro.Controls"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        ResizeMode="NoResize" 
        Title="{x:Static local:Resources.FormHelpTitle}" Height="350" Width="350"
        WindowStartupLocation="CenterOwner">
    
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="/BedrockEditorPro;component/BedrockStyle.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component/XamlImages.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </controls:ProWindow.Resources>
    
    <Grid Style="{DynamicResource BedrockGrid}" Margin="10" VerticalAlignment="Top">

        <Grid.RowDefinitions>

            <!--Addin infomation-->
            <RowDefinition Height="120"/>
            
            <!--Help-->
            <RowDefinition Height="180"/>

            <!--More-->
            <!--<RowDefinition Height="120"/>-->

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" >
            <Label Content="{x:Static local:Resources.FormHelpFirstSection}" FontWeight="Bold"/>
            <Label Margin="5" Content="{Binding AddinVersion}" />
            <Label Margin="5" Content="{Binding DatabaseVersion}" />
        </StackPanel>

        <StackPanel Grid.Row="1" >
            <Label Content="{x:Static local:Resources.FormHelpSecondSection}" FontWeight="Bold"/>
            <Button x:Name="ButtonHelpProject" Command="{Binding OpenOnlineProject}" Content="{x:Static local:Resources.FormHelpOpenProject}" Style="{DynamicResource Esri_Button}"/>
            <Button x:Name="ButtonHelpIssue" Command="{Binding OpenOnlineIssue}" Content="{x:Static local:Resources.FormHelpOpenIssue}" Style="{DynamicResource Esri_Button}"/>
            <Button x:Name="ButtonHelpGuide" Command="{Binding OpenOnlineGuide}" Content="{x:Static local:Resources.FormHelpOpenGuide}" Style="{DynamicResource Esri_Button}"/>
            <Button x:Name="ButtonHelpModel" Command="{Binding OpenOnlineModel}" Content="{x:Static local:Resources.FormHelpOpenModel}" Style="{DynamicResource Esri_Button}"/>

        </StackPanel>


        <!--<StackPanel Grid.Row="2">
            <Label Content="{x:Static local:Resources.FormHelpThirdSection}" FontWeight="Bold"/>
        </StackPanel>-->

    </Grid>
</controls:ProWindow>
