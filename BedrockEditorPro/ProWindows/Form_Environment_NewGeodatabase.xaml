<controls:ProWindow x:Class="BedrockEditorPro.ProWindows.Form_Environment_NewGeodatabase"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BedrockEditorPro.Properties"
        xmlns:bedrockcontrols="clr-namespace:BedrockEditorPro.Controls"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions" 
        mc:Ignorable="d"
        ResizeMode="NoResize" 
        Title="{x:Static local:Resources.FormEnvironmentNewGeodatabaseTitle}" MinHeight="310" Width="550"
        WindowStartupLocation="CenterOwner">
    
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="/BedrockEditorPro;component/BedrockStyle.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component/XamlImages.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid Style="{DynamicResource BedrockGrid}" Margin="10,10,10,10" VerticalAlignment="Center">

        <!--Create Project GDB from Master Schema-->
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Row="0" IsChecked="True" Content="{x:Static local:Resources.FormEnvironmentNewGeodatabaseCheckbox}" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <!--Manual xml file entry-->
        <Label Grid.Row="1" Content="{x:Static local:Resources.FormEnvironmentNewGeodatabaseInput1}" Style="{DynamicResource BedrockButtonLabel}" IsEnabled="False"/>
        <TextBox Grid.Row="2" Style="{DynamicResource BedrockTextboxInputs}" IsEnabled="False" Margin="0,0,10,0" Height="25"/>
        <bedrockcontrols:BrowseButton Grid.Row="2" Grid.Column="1" IsEnabled="false"/>
        
        <!--Output-->
        <Label Grid.Row="3" Content="{x:Static local:Resources.FormEnvironmentNewGeodatabaseInput2}" Style="{DynamicResource BedrockButtonLabel}" />
        <TextBox Grid.Row="4" Style="{DynamicResource BedrockTextboxInputs}" Margin="0,0,10,0" Height="25"/>
        <bedrockcontrols:BrowseButton  Grid.Row="4" Grid.Column="1"/>

        <!--Projection-->
        <Label Grid.Row="5" Content="{x:Static local:Resources.FormEnvironmentNewGeodatabaseInput3}" Style="{DynamicResource BedrockButtonLabel}" />
        <TextBox Grid.Row="6" Style="{DynamicResource BedrockTextboxInputs}" Margin="0,0,10,0" Height="25"/>
        <Button Grid.Row="6" Grid.Column="1" Style="{DynamicResource Esri_Button}" Margin="5" VerticalAlignment="Center" HorizontalContentAlignment="Right">
            <Button.Content>
                <Border>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Image Source="{DynamicResource CoordinateSystem16}"  Margin="3" HorizontalAlignment="Center" Width="16" Height="16"/>
                        <TextBlock Text="{x:Static local:Resources.GenericButtonBrowse}" VerticalAlignment="Center" Margin="5,5,5,5" />
                    </StackPanel>
                </Border>
            </Button.Content>
        </Button>

        <!--Save/Cancel-->
        <Button Grid.Row="8" Content="{x:Static local:Resources.GenericButtonSave}" Style="{DynamicResource BedrockButtonSave}" HorizontalAlignment="Left" Width="100"/>
        <bedrockcontrols:CancelButton Grid.Row="8" />

    </Grid>
</controls:ProWindow>
