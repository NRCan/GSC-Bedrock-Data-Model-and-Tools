<controls:ProWindow x:Class="BedrockEditorPro.ProWindows.Form_Environment_NewGeodatabase"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:ArcGIS.Desktop.Framework.Controls;assembly=ArcGIS.Desktop.Framework"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:extensions="clr-namespace:ArcGIS.Desktop.Extensions;assembly=ArcGIS.Desktop.Extensions"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Create Project GDB from Master Schema" Height="275" Width="500"
        WindowStartupLocation="CenterOwner"
    >
    <controls:ProWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <extensions:DesignOnlyResourceDictionary Source="pack://application:,,,/ArcGIS.Desktop.Framework;component\Themes\Default.xaml"/>
                <extensions:DesignOnlyResourceDictionary Source="/BedrockEditorPro;component/BedrockStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </controls:ProWindow.Resources>
    <Grid Style="{DynamicResource BedrockGrid}" Margin="10,10,10,10">

        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <CheckBox Grid.Row="0" IsChecked="True" Content="Default Embedded (XML Version 10.2)" HorizontalAlignment="Left" VerticalAlignment="Center"/>

        <!--Manual xml file entry-->
        <Label Grid.Row="1" Content="Input Project Template (.xml)" Style="{DynamicResource BedrockButtonLabel}" IsEnabled="False"/>
        <TextBox Grid.Row="2" Style="{DynamicResource BedrockTextboxInputs}" IsEnabled="False"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Browse" HorizontalAlignment="Center" VerticalAlignment="Stretch" IsEnabled="False"/>

        <!--Output-->
        <Label Grid.Row="3" Content="Output Database Path" Style="{DynamicResource BedrockButtonLabel}" />
        <TextBox Grid.Row="4" Style="{DynamicResource BedrockTextboxInputs}" />
        <Button Grid.Row="4" Grid.Column="1" Content="Browse" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <!--Projection-->
        <Label Grid.Row="5" Content="Projection" Style="{DynamicResource BedrockButtonLabel}" />
        <TextBox Grid.Row="6" Style="{DynamicResource BedrockTextboxInputs}"/>
        <Button Grid.Row="6" Grid.Column="1" Content="Browse" HorizontalAlignment="Center" VerticalAlignment="Stretch" />

        <!--Save/Cancel-->
        <Button Grid.Row="8" Content="Save" Style="{DynamicResource BedrockButtonSave}" HorizontalAlignment="Left"/>
        <Button Grid.Row="8" Content="Cancel" Style="{DynamicResource BedrockButtonCancel}" HorizontalAlignment="Right"/>

    </Grid>
</controls:ProWindow>
